{
  "name": "uni-market",
  "version": "0.4.0",
  "private": true,
  "description": "WeChat Mini Program marketplace MVP for international students in Hangzhou.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/theycallmedern/uni-market.git"
  },
  "homepage": "https://github.com/theycallmedern/uni-market",
  "bugs": {
    "url": "https://github.com/theycallmedern/uni-market/issues"
  },
  "scripts": {
    "check": "bash scripts/check.sh",
    "cf:dev": "npx wrangler dev --env dev",
    "cf:deploy:dev": "npx wrangler deploy --env dev",
    "cf:d1:migrate:dev": "npx wrangler d1 migrations apply unimarket-dev --env dev --remote",
    "cf:d1:studio:dev": "npx wrangler d1 execute unimarket-dev --env dev --command='SELECT name FROM sqlite_master WHERE type = \"table\" ORDER BY name;'",
    "cf:deploy:prod": "npx wrangler deploy --env production",
    "cf:d1:migrate:prod": "npx wrangler d1 migrations apply unimarket-prod --env production --remote"
  }
}
